# PAUL Handoff

**Date:** 2026-03-04 (fourth pause — context limit)
**Status:** Data pipeline fixes deployed. Calendar diagnosis pending. L-02-05 needs planning.

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Core value:** One system that knows everything, surfaces what matters, keeps Jonathan on track.
**Production:** https://jarvis.whatamiappreciatingnow.com/

---

## Current State

**Version:** v4.4 — Guided Onboarding (Academy-Driven)
**Phase:** L-02 of 4 — Live Walkthrough Pass 1
**Plan:** L-02-04 — APPLIED. UNIFY still pending. New bugs found in walkthrough → need L-02-05.

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     ← skip UNIFY, go straight to L-02-05 planning (new bugs found)
```

---

## What Was Done This Session

### 1. Bills Static Fallback — DEPLOYED (commit d914f2a)
- Created `src/lib/jarvis/data/staticBills.ts` — Jonathan's full 28-bill recurring list organized by day-of-month
- BriefingBuilder now falls back to static bills when Notion returns empty
- Bills will NOW show in the app at /jarvis/app/personal/bills regardless of Notion
- Fixed dead-code bug in `transformBills` status logic (due_soon branch was unreachable)
- **Day 1 bills show as OVERDUE (March 1 passed). Day 5 (AmEx $385, Jennie Barclay $131, TJ Maxx) show as DUE SOON.**

### 2. Calendar Diagnostic — DEPLOYED (commit 3677814)
- Added `calendar` section to `/api/jarvis/health` endpoint
- Now returns: `{ configured, eventsToday, error }` — was completely blind before
- Hit https://jarvis.whatamiappreciatingnow.com/api/jarvis/health to see actual calendar error

### 3. Agent Capabilities Concept Doc — CREATED
- `jarvis/.paul/concepts/jarvis-agent-capabilities.md`
- 8 paradigms documented (not just Playwright — screenshot/vision, DOM, hybrid, managed infra, API-first, MCP servers, frameworks, telephony)
- Jonathan's real use cases: credit report audit, entity filings, family trust prep, pizza ordering
- Research questions flagged before planning can begin
- v4.5 milestone: "Jarvis Hands"

---

## L-02-05 Bug List (from Jonathan's walkthrough today)

These need to be planned as L-02-05:

| # | Bug | Type |
|---|-----|------|
| 1 | Continue chip still not appearing during tutorial | Regression |
| 2 | Tutorial ran only 2 topics then stopped — no deterministic curriculum | Architectural |
| 3 | Personal vs Home nav — not tested (tutorial bailed early) | Blocked by #2 |
| 4 | ✓ Center + button opens chat | FIXED |
| 5 | Chat doesn't scroll to bottom when opened | Still broken |
| 6 | Chat stays open when spotlight appears — should close first | New bug |
| 7 | Toggling speaker starts second voice from beginning — double TTS | New bug |
| 8 | Tasks list bottom buried under nav bar — safe-area still broken | Still broken |
| 9 | Voice entry is 5 steps — lost touch-and-talk UX | UX regression |

**#2 is the most important.** The tutorial needs a FIXED, DETERMINISTIC curriculum — not AI-improvised. Without this, we can't evaluate if the tutorial is working.

---

## Critical: Calendar Still Broken

Env vars ARE set in Vercel (confirmed in screenshot, Added Mar 1). Yet nothing shows.
To diagnose: hit https://jarvis.whatamiappreciatingnow.com/api/jarvis/health
Look at the `calendar` field — it will show the actual error for the first time.

Most likely causes (in order):
1. `GOOGLE_CALENDAR_SERVICE_ACCOUNT_JSON` has whitespace/newline issues (like NOTION_BILLS_DATA_SOURCE_ID did) — JSON.parse fails silently
2. Service account was never shared on the actual Google Calendars (sharing wasn't done)
3. JWT signing fails in Vercel production environment

**DO NOT ask Jonathan to repeat setup steps without reading the health endpoint error first.**

---

## Weekend Priority (2 days, Jonathan working both)

Jonathan wants to show Jenny the app. Priority order:
1. Fix calendar (check health endpoint error first)
2. Fix safe-area/bottom nav padding (site-wide, embarrassing)
3. Build deterministic tutorial curriculum (3-5 stops: Briefing → Chat → Tasks/Bills/Calendar)
4. Fix L-02-05 bugs (Continue chip, chat close before spotlight, double TTS, chat scroll)

Jonathan's vision for tutorial: Morning Briefing → Calendar → Tasks → Bills (all shown together in briefing). Jenny needs to see real data. Static bills are now working. Calendar still needs fixing.

---

## NOTION_BILLS_DATA_SOURCE_ID Whitespace

Jonathan confirmed the whitespace warning in Vercel on `NOTION_BILLS_DATA_SOURCE_ID`.
The value is: `0ab02093-f0b3-8219-99da-8722861f036b` (has newline chars around it)
This needs to be cleaned up — but bills already work via static fallback so this is NOT urgent.
**When raising this: do NOT make it sound like something Jonathan hasn't done yet. It's a Vercel paste artifact.**

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `jarvis/.paul/concepts/jarvis-agent-capabilities.md` | Agent vision — 8 paradigms, research needed |
| `src/lib/jarvis/data/staticBills.ts` | Jonathan's 28 recurring bills — static fallback |
| `src/lib/jarvis/executive/BriefingBuilder.ts` | Bills fallback injection (lines ~162, ~777) |
| `src/app/api/jarvis/health/route.ts` | Calendar diagnostic now included |

---

## CRITICAL Reminders

- PAUL lives at `jarvis/.paul/` (not repo root `.paul/`)
- No local dev servers — push to GitHub → auto-deploys → test from live site
- `ELEVENLABS_API_KEY` is in Vercel — DO NOT ask Jonathan to add it again
- `GOOGLE_CALENDAR_ID` and `GOOGLE_CALENDAR_SERVICE_ACCOUNT_JSON` are BOTH set in Vercel — DO NOT ask Jonathan to set them again
- Bills env var (`NOTION_BILLS_DATA_SOURCE_ID`) has whitespace — Jonathan knows, not urgent (static fallback works)
- Jonathan is FRUSTRATED with duplicate instructions — check notes before asking him to do anything

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Hit https://jarvis.whatamiappreciatingnow.com/api/jarvis/health — read the `calendar.error` field
3. Ask Jonathan how bills look (should be populated now)
4. Decide: fix calendar first, OR go straight to curriculum design + L-02-05 planning
5. Jonathan's weekend goal: show Jenny a working demo with real data

---

*Handoff created: 2026-03-04 — context limit, bills fixed, calendar diagnostic deployed*
