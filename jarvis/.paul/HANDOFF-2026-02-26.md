# PAUL Handoff

**Date:** 2026-02-26 (updated after E-04-04)
**Status:** Clean pause — loop closed, ready for E-04-05 planning

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis v4.0 — Brain Swap & Life Manager UI
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.
**Production:** https://jarvis.whatamiappreciatingnow.com/

---

## Current State

**Milestone:** v4.0 Brain Swap & Life Manager UI
**Phase:** E of G — Multi-Domain Operating System (Mobile-First UI Redesign)
**Plan:** E-04-04 complete — ready to plan E-04-05
**Progress:** Milestone ~80%, Phase E ~70%

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop complete — ready for next PLAN]
```

---

## What Was Done This Session

- **E-04-04 planned and executed** — Settings page + dynamic domain activation
- Created `settingsStore.ts` (zustand + persist): activeDomainIds, notificationMode, featureToggles
- Exported `useActiveDomains()` hook — canonical API for reactive domain filtering
- Built full Settings page with 4 sections: Domains (toggle per domain), Notifications (2x2 mode card selector), Features (toggle rows), About
- Wired DomainRail + DomainHealthGrid to read from settingsStore instead of hardcoded domains.ts
- Protected domains: Home and Personal cannot be deactivated
- 277 new lines, zero new TS errors, committed and pushed

---

## What's In Progress

Nothing — clean loop closure. E-04-04 is fully complete with SUMMARY.

---

## What's Next

**Immediate:** Plan E-04-05 — Personal domain dashboard + sub-views

The Personal domain is the first real data domain. The IA doc (lines 275-348) specifies 7 sub-programs:
- Tasks, Habits, Bills & Finance, Calendar, Journal, Goals & Planning, Health & Wellness
- Each backed by Notion DBs (proxy pattern — display only, no data duplication)
- Dashboard shows: today's tasks, habit checklist, bills summary, calendar preview, quick stats

**After E-04-05:** Empty/error state patterns, onboarding flow, notification foundation

**After all E-04:** E-05 Build Wave 2 (external domain codebases — Reset Bio, Visopscreen, etc.)

---

## Three Blueprint Documents (Read before any E-04+ work)

These are the architecture bibles — every UI decision traces back to them:

1. `.paul/research/phase-e-domain-atlas.md` — What exists across all 5 codebases + Notion
2. `.paul/research/phase-e-information-architecture.md` — How domains connect (v1.1, ~1600 lines)
3. `.paul/research/phase-e-ui-system-design.md` — How everything looks (v1.0, ~900 lines)

---

## Cumulative E-04 Build (01 through 04)

| Sub-phase | What | Lines | Files |
|-----------|------|-------|-------|
| E-04-01 | Shell, DomainRail, BottomTabBar, 4 primitives | 761 | 17 |
| E-04-02 | Priority Home, 5 composites, widget system | 682 | 12 |
| E-04-03 | 4 primitives (8/8), ChatOverlay, toast system | 1,065 | 10 |
| E-04-04 | settingsStore, Settings page, domain wiring | 277 | 4 |
| **Total** | | **~2,800** | **~43** |

### Stores (4 total now)
| Store | File | Purpose |
|-------|------|---------|
| shellStore | `src/lib/jarvis/stores/shellStore.ts` | Shell state (domain, chat, command palette) |
| homeStore | `src/lib/jarvis/stores/homeStore.ts` | Home data (priorities, health, widgets) |
| toastStore | `src/lib/jarvis/stores/toastStore.ts` | Toast notifications + convenience API |
| **settingsStore** | `src/lib/jarvis/stores/settingsStore.ts` | **Domain activation, notification mode, feature toggles** |

### Key Patterns Established
- **Spring easing:** `cubic-bezier(0.34, 1.56, 0.64, 1)` — used everywhere
- **Touch gestures:** Ref-based tracking (no React state during drag) for 60fps
- **CSS animations:** Keyframes via `<style>` tags in components, no libraries
- **Domain colors:** DOMAIN_COLORS lookup for safe Tailwind dynamic classes
- **Icons:** DomainIcon helper resolves string names to lucide components
- **useActiveDomains():** Canonical way to get active domains (replaces getActiveDomains())

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (A-G) |
| `.paul/phases/E-mobile-ui/E-04-04-SUMMARY.md` | Latest completed plan summary |
| `.paul/research/phase-e-information-architecture.md` | IA blueprint |
| `.paul/research/phase-e-ui-system-design.md` | UI design blueprint |
| `src/lib/jarvis/stores/settingsStore.ts` | Settings store + useActiveDomains hook |
| `src/lib/jarvis/domains.ts` | Domain config (8 domains, colors, routes) |

---

## Known Issues / Technical Debt

- **Pre-existing build failure:** `audio-prep` module missing deps (`music-metadata`, `@distube/ytdl-core`) — unrelated to Jarvis, does NOT affect deployment
- **SSE streaming duplication:** ChatOverlay copies streaming logic from ChatPanel — two copies exist. DRY opportunity for later.
- **No markdown rendering** in chat messages yet — plain text only
- **Old /jarvis route untouched** — parallel with new /jarvis/app
- **domains.ts `active` field now redundant** — settingsStore is source of truth, but field kept as initial default

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Read this handoff for full context
3. Run `/paul:resume` or `/paul:progress`
4. Next action: `/paul:plan` for E-04-05

---

*Handoff created: 2026-02-26 (updated)*
