# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — session break after UNIFY (clean pause between loops)

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis — Self-Improving Life Manager
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-01 of 4 — Foundation
**Plan:** L-01-01 complete (unified), L-01-02 not yet written

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [Loop complete — ready for next PLAN]
```

---

## What Was Done

This session:
- Resumed from handoff, confirmed L-01-01 APPLY was complete
- Ran `/paul:unify` — reconciled plan vs actual, created L-01-01-SUMMARY.md
- All 3 acceptance criteria passed (redirect, curriculum, handoff)
- Documented 5 auto-fixed deviations (keyFile path corrections from previous session's self-audit)
- Archived previous handoff to `.paul/handoffs/archive/`

Previous session (L-01-01 APPLY):
- Middleware redirect: subdomain root → `/jarvis/app`, main domain `/jarvis` → `/jarvis/app`
- Jarvis Academy curriculum: 8 topics (2 Tier 0 "First Contact" + 6 Tier 1 "Your First Day")
- OnboardingWizard → Academy handoff: both "Start Tour" and "Skip" set activeProject to 'jarvis'
- Self-audit fixed 5 wrong keyFile paths (basePath: 'src', not 'jarvis/src/components/jarvis')

---

## What's In Progress

- Nothing partially built — clean pause at loop boundary

---

## What's Next

**Immediate:** Run `/paul:plan` to create L-01-02-PLAN.md for Spotlight Bridge + Enhanced Teaching Context

**What L-01-02 should cover:**
- Spotlight Bridge: wire `data-tutorial-id` attributes to DOM elements so Academy topics can highlight them
- Enhanced Teaching Context: same-origin interactive teaching where Academy can verify user actions
- Differentiate `startTour()` from `finishOnboarding()` (currently both just call finishOnboarding)
- The `spotlightTargets` and `verificationSteps` arrays on curriculum topics are the data contract — L-01-02 builds the runtime

**After that:** L-02 Live Walkthrough Pass 1 (Jonathan walks through, we fix what breaks)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview — v4.4 with 4 phases |
| `.paul/phases/L-01-foundation/L-01-01-PLAN.md` | Executed plan — redirect + curriculum + handoff |
| `.paul/phases/L-01-foundation/L-01-01-SUMMARY.md` | Unification results — all AC passed |
| `.paul/concepts/guided-onboarding-v44.md` | Full concept doc with architecture, curriculum spec |
| `src/middleware.ts` | Redirect logic (modified in L-01-01) |
| `src/lib/jarvis/academy/projects.ts` | Jarvis curriculum — 8 topics, basePath: 'src' |
| `src/components/jarvis/onboarding/OnboardingWizard.tsx` | Academy handoff on completion |

---

## Key Context for Next Session

- `basePath: 'src'` is the correct pattern for Jarvis curriculum (not 'jarvis/src/components/jarvis')
- `useTutorialStore` import is intentionally kept in OnboardingWizard — Plan 02 needs it for spotlight
- `startTour()` currently equals `finishOnboarding()` — Plan 02 must differentiate them
- `spotlightTargets` on topics reference `data-tutorial-id` attributes that DON'T EXIST YET on DOM elements
- Build passes clean. No type errors.
- STATE.md and SUMMARY are uncommitted (only PAUL metadata, no src changes)

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Loop position: PLAN ✓, APPLY ✓, UNIFY ✓ (complete)
3. Run `/paul:plan` to create L-01-02 (Spotlight Bridge + Teaching Context)

---

*Handoff created: 2026-03-02*
