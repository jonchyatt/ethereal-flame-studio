# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — session break after APPLY + self-audit

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis — Self-Improving Life Manager
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-01 of 4 — Foundation
**Plan:** L-01-01 — APPLY complete (3/3 tasks), awaiting UNIFY

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [Run UNIFY next]
```

---

## What Was Done

- **Task 1:** Middleware redirect — subdomain root rewrites `/` → `/jarvis/app` (was `/jarvis`), main domain redirects `/jarvis` → `/jarvis/app`
- **Task 2:** Jarvis Academy curriculum — CurriculumTopic interface extended with `spotlightTargets?` + `verificationSteps?`. `jarvis` project registered in ACADEMY_PROJECTS with 8 topics (2 First Contact + 6 Your First Day)
- **Task 3:** OnboardingWizard → Academy handoff — `finishOnboarding()` calls `academyStore.setActiveProject('jarvis')`, both "Start Tour" and "Skip" paths hand off to Academy
- **Self-audit (critical):** Found and fixed 5 data accuracy bugs in curriculum keyFile paths:
  - basePath was `jarvis/src/components/jarvis` (no `jarvis/` dir exists) → fixed to `src`
  - `CommandPalette.tsx` → `components/jarvis/layout/CommandPalette.tsx`
  - `chat/ChatOverlay.tsx` → `components/jarvis/layout/ChatOverlay.tsx`
  - `brain/tools/briefing.ts` (fabricated) → `app/api/jarvis/briefing/route.ts`
  - `stores/personalStore.ts` → `lib/jarvis/stores/personalStore.ts`
  - All 12 keyFile paths verified against real files on disk

---

## What's In Progress

- Nothing partially built — clean pause point between APPLY and UNIFY

---

## What's Next

**Immediate:** Run `/paul:unify` to close the L-01-01 loop (reconcile plan vs actual, create SUMMARY)

**After that:** Create L-01-02-PLAN.md for Spotlight Bridge + Enhanced Teaching Context (same-origin interactive teaching — the Academy can highlight DOM elements and verify user actions)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview — v4.4 milestone with 4 phases |
| `.paul/phases/L-01-foundation/L-01-01-PLAN.md` | Plan that was executed — redirect + curriculum + handoff |
| `.paul/concepts/guided-onboarding-v44.md` | Full concept doc with architecture, curriculum spec, testing methodology |
| `src/middleware.ts` | Modified — redirect logic |
| `src/lib/jarvis/academy/projects.ts` | Modified — jarvis curriculum (8 topics, basePath: 'src') |
| `src/components/jarvis/onboarding/OnboardingWizard.tsx` | Modified — Academy handoff on completion |

---

## Key Context for Next Session

- The self-audit was important: initial APPLY had all paths wrong due to basePath mismatch. Fixed to `basePath: 'src'` with full relative paths. This matches how the toolExecutor resolves paths via GitHub API.
- `useTutorialStore` import is kept in OnboardingWizard intentionally (Plan 02 will use it for spotlight)
- `startTour()` is now just a pass-through to `finishOnboarding()` — Plan 02 will differentiate them again with spotlight behavior
- Build passes clean. No type errors.
- Previous handoff was archived to `.paul/handoffs/archive/HANDOFF-2026-03-02.md`

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Check loop position (should be: PLAN ✓, APPLY ✓, UNIFY ○)
3. Run `/paul:unify` to close the L-01-01 loop

---

*Handoff created: 2026-03-02*
