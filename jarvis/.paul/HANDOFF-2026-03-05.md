# PAUL Handoff

**Date:** 2026-03-05 (context limit)
**Status:** Calendar confirmed working. L-02-05 plans written. Ready to execute.

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Production:** https://jarvis.whatamiappreciatingnow.com/

---

## What Was Done This Session

### 1. Calendar — CONFIRMED WORKING
- `darkjjjr@gmail.com`: 200 OK, 7 events in last 30 days, "Jarvis test" (tomorrow) = 8th event picked up ✓
- `family13355774524516286647@group.calendar.google.com`: 200 OK, 0 events (Jenny uses Awesome Calendar / iCloud, not Google)
- Calendar was NEVER broken — just no events on tested days
- Health endpoint improved: now queries -30d to +7d, shows per-calendar status+eventCount, shows queryWindow

### 2. Root Cause of Tutorial Stopping Found
- `bills-4-mark-paid` step uses store verification: `personalStore.bills.some(b => b.status === "paid")`
- Static bills CANNOT be marked paid — no UI action, no write-back
- This step PERMANENTLY fails → tutorial stuck after habits lesson forever
- This is why "tutorial ran only 2 topics then stopped"

### 3. Two L-02-05 Plans Written
- `jarvis/.paul/phases/L-02-live-walkthrough-pass1/L-02-05-01-PLAN.md` — Tutorial fixes
- `jarvis/.paul/phases/L-02-live-walkthrough-pass1/L-02-05-02-PLAN.md` — Chat/TTS bugs

---

## L-02-05-01 Plan Summary (execute first)

**3 tasks, all auto:**
1. Fix `bills-4-mark-paid` → replace with route-based observation step
2. Add 45s step timeout to TutorialEngine (prevents any future stall)
3. Float Continue chip at JarvisShell level (remove from ChatOverlay — it was hidden when chat closed)

**Files:** `tutorialLessons.ts`, `useTutorialEngine.ts`, `JarvisShell.tsx`, `ChatOverlay.tsx`

## L-02-05-02 Plan Summary (execute after 01)

**2 tasks, all auto:**
1. Close chat when spotlight fires + fix scroll-to-bottom timing
2. Shared TTS audio singleton — prevents double-playback, prevents narration restart on toggle

**Files:** `ChatOverlay.tsx`, `SpotlightOverlay.tsx`

---

## Still Needs Plans (L-02-05-03 not written yet)

- Safe-area: tasks list bottom buried under nav (JarvisShell `pb-[calc(5rem+...)]` may need adjustment)
- Voice touch-and-talk: currently 5 steps, target press-and-hold
- CalendarView month grid (lower priority for Jenny demo)

---

## Jenny Demo Priorities

1. ✅ Bills show (static fallback working)
2. ✅ Calendar shows when there are events
3. ⬜ Tutorial runs all 4 lessons without stopping (L-02-05-01)
4. ⬜ Tutorial UX smooth — no double TTS, chat doesn't overlap spotlight (L-02-05-02)
5. ⬜ Safe-area/task list not buried (L-02-05-03)

---

## Key Reminders

- PAUL lives at `jarvis/.paul/` (not repo root)
- No local dev — push to GitHub → auto-deploys
- Calendar IS working — don't re-investigate
- Jenny's calendar = Awesome Calendar (likely iCloud) — not in Google Calendar
- `ELEVENLABS_API_KEY` already in Vercel — never ask again
- All Google Calendar env vars set — never ask again

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Run `/paul:apply jarvis/.paul/phases/L-02-live-walkthrough-pass1/L-02-05-01-PLAN.md`
3. Then `/paul:apply jarvis/.paul/phases/L-02-live-walkthrough-pass1/L-02-05-02-PLAN.md`
4. Then plan L-02-05-03 (safe-area + voice)

*Handoff: 2026-03-05 — context limit, plans ready to execute*
