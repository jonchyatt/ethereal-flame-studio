# PAUL Handoff

**Date:** 2026-03-04 (third pause — context limit)
**Status:** L-02-04 deployed. Jonathan testing on phone. Awaiting UNIFY.

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Core value:** One system that knows everything, surfaces what matters, keeps Jonathan on track.
**Production:** https://jarvis.whatamiappreciatingnow.com/

---

## Current State

**Version:** v4.4 — Guided Onboarding (Academy-Driven)
**Phase:** L-02 of 4 — Live Walkthrough Pass 1
**Plan:** L-02-04 — APPLIED and deployed (commits 62160b7, 2e3b9b2)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     ← awaiting device walkthrough, then UNIFY
```

---

## What Was Done This Session

### 1. Vision Architecture — Three Tiers Designed & Documented
- Discussed "Vision Claw" concept — giving Jarvis the ability to see what's on screen
- Designed three tiers:
  - **Tier 1:** Dynamic route context (no screenshots, ~$0) — SHIPPED THIS SESSION
  - **Tier 2:** On-demand `getDisplayMedia()` screenshots — queued, iOS barrier (Jonathan owns)
  - **Tier 3:** Ambient screen monitoring — requires native wrapper, Jonathan owns
- Updated `jarvis/.paul/concepts/jarvis-screen-vision.md` with full tier breakdown,
  technical barriers, and Jonathan's commitment to solve them

### 2. All 9 L-02-04 Bugs Fixed (commit 62160b7)

| Bug | What Changed | File |
|-----|-------------|------|
| 1+2 | Continue chip calls `engine.skipStep()` directly — no API call, always visible during tutorial | `ChatOverlay.tsx` |
| 3 | APP NAVIGATION map added to system prompt — Claude knows Personal ≠ Home | `systemPrompt.ts` |
| 4 | Removed `safe-area-all` from JarvisLayout, increased `main` pb 4rem→5rem — content no longer clipped under nav | `layout.tsx`, `JarvisShell.tsx` |
| 6 | `+` center button → `toggleChat()` instead of `console.log` | `BottomTabBar.tsx` |
| 7 | Chat auto-scrolls to bottom when opened (was always opening at top) | `ChatOverlay.tsx` |
| 8 | Academy shows "Tutorial in progress" banner + Return button when tutorial is active | `AcademyHub.tsx` |
| 9 | Spotlight tap on ring-type steps emits bus event → advances immediately (no 3s wait) | `SpotlightOverlay.tsx`, `useTutorialEngine.ts` |
| 10 | Auto-fixed by BUG 1 (Continue chip no longer calls Claude API) | — |

### 3. Tier 1 Vision Shipped (commit 2e3b9b2)
- `ChatOverlay.tsx`: `usePathname()` → `buildAppContext()` → `appContext` field in every API request
- `chat/route.ts`: extracts `appContext`, appends to system prompt as `CURRENT APP LOCATION: ...`
- Claude now knows exactly which screen the user is on for every message
- Navigation bugs like "go to Home" (meaning Personal) are structurally impossible

---

## What's In Progress

Jonathan is testing on phone. No code changes pending. Waiting for:
1. Device walkthrough to confirm all 9 bugs are resolved
2. TTS voice test — standard voice `XjLkpWUlnhS8i7gGz3lZ` was swapped in prev session

---

## What's Next

**Immediate:** `/paul:unify` after Jonathan reports walkthrough results

The UNIFY step will:
- Compare L-02-04 plan vs actual (what was planned, what shipped, any divergence)
- Update STATE.md with phase completion status
- Decide: does L-02 need another pass (L-02-05), or is it done and we move to L-03?

**After UNIFY:**
- If bugs resolved → L-02 COMPLETE → plan L-03 (Live Walkthrough Pass 2 — wife's perspective)
- If new bugs found → document them → L-02-05 fix pass

---

## Test Checklist (for device walkthrough)

Jonathan should verify:
- [ ] Continue chip appears during tutorial and taps advance the step (no API call)
- [ ] Jarvis says "go to Personal" not "go to Home" when directing to tasks/habits
- [ ] Briefing card is fully visible (not clipped under bottom nav)
- [ ] `+` center button opens chat
- [ ] Chat opens scrolled to bottom (not top of history)
- [ ] Tapping Learn during tutorial shows "Tutorial in progress" banner
- [ ] Tapping a spotlight ring advances the step immediately (no 3s wait)
- [ ] TTS narration — does voice play? (ElevenLabs standard voice test)

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `jarvis/.paul/PROJECT.md` | Architecture, decisions, requirements |
| `jarvis/.paul/concepts/jarvis-screen-vision.md` | Vision Tier 1/2/3 concept + barriers |
| `src/components/jarvis/layout/ChatOverlay.tsx` | Continue chip fix, scroll fix, Tier 1 client side |
| `src/lib/jarvis/intelligence/systemPrompt.ts` | Nav map (BUG 3) |
| `src/app/api/jarvis/chat/route.ts` | Tier 1 route — appends appContext to system prompt |

---

## CRITICAL Reminders

- PAUL lives at `jarvis/.paul/` (not repo root `.paul/`)
- No local dev servers — push to GitHub → auto-deploys → test from live site
- `ELEVENLABS_API_KEY` is in Vercel — DO NOT ask Jonathan to add it again
- TTS now uses hardcoded standard voice `XjLkpWUlnhS8i7gGz3lZ` (not clone) — test on device
- Git: commit directly to master, single dev, no branches needed

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Read this file
3. Ask Jonathan if the device walkthrough showed any new bugs
4. If clean → run `/paul:unify` to close L-02-04
5. If new bugs → document them → plan L-02-05

---

*Handoff created: 2026-03-04 — context limit, L-02-04 deployed, testing in progress*
