# PAUL Handoff

**Date:** 2026-03-05 (session pause)
**Status:** paused — L-02-05-03 needs planning

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Production:** https://jarvis.whatamiappreciatingnow.com/
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-02 of 4 (Live Walkthrough Pass 1) — In Progress
**Plan:** L-02-05-01 ✓, L-02-05-02 ✓, L-02-05-03 not yet planned

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [L-02-05-01 complete]
  ✓        ✓        ✓     [L-02-05-02 complete]
  ○        ○        ○     [L-02-05-03 not yet planned]
```

---

## What Was Done This Session

### L-02-05-01 (commit 4196320) — Tutorial curriculum fix
- Replaced `bills-4-mark-paid` step (store verification against static bills = PERMANENTLY FAILS) with `bills-4-understand-totals` (route verification — stay on page = success)
- Added 45s step timeout (`stepTimeoutRef`) to `useTutorialEngine` — auto-advances stuck steps with "Let's move on" message
- Moved Continue button from `ChatActionsRow` (hidden when chat closed) to `JarvisShell` (`TutorialContinueButton`) — always visible above bottom tab bar at `z-[10002]`
- Removed stale bills storeVerifier from `verificationEngine.ts`

### L-02-05-02 (commit ef76479) — Chat/TTS bugs
- Chat closes when spotlight activates (`useEffect` on `spotlight` → `closeChat()`) — no more overlapping UIs
- Scroll-to-bottom on chat open: 50ms timeout → `requestAnimationFrame`, added `isChatOpen` to auto-scroll deps
- Shared TTS audio manager: `activeTTSAudio` singleton + `stopActiveTTS()` exported from `SpotlightOverlay.tsx`, imported by `ChatOverlay.tsx`. One audio track at a time — spotlight cancels chat TTS and vice versa
- SpotlightOverlay narration: removed `isNarrationEnabled` from effect deps — toggle no longer restarts narration from beginning

---

## Jenny Demo Checklist

1. ✅ Bills show (static fallback)
2. ✅ Calendar shows when events exist
3. ✅ Tutorial runs all 4 lessons without stopping (L-02-05-01)
4. ✅ Tutorial UX smooth — no double TTS, chat closes for spotlight (L-02-05-02)
5. ⬜ Tasks list visible — not buried under nav (L-02-05-03)
6. ⬜ Voice touch-and-talk UX (L-02-05-03, lower priority)

---

## What's Next

**Immediate:** Plan and execute L-02-05-03

**L-02-05-03 scope:**
1. **Safe-area fix** — tasks list bottom items buried under bottom tab bar (`pb-[calc(5rem+env(safe-area-inset-bottom))]` in `JarvisShell` `<main>` — check if this is the right fix or if individual pages need padding)
2. **Voice touch-and-talk** — tutorial currently has 5 steps for voice (open chat, tap mic, press, speak, release), target is press-and-hold gesture. Currently lower priority than safe-area.

**After that:** `/paul:unify` all L-02-05 plans (01+02+03), then assess L-02 completion. If tutorial is now working end-to-end, move to L-03.

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `src/lib/jarvis/curriculum/tutorialLessons.ts` | Tutorial lesson definitions (fixed bills step) |
| `src/lib/jarvis/hooks/useTutorialEngine.ts` | Tutorial orchestration (has 45s timeout) |
| `src/components/jarvis/layout/JarvisShell.tsx` | Shell — TutorialContinueButton lives here now |
| `src/components/jarvis/layout/ChatOverlay.tsx` | Chat — closes on spotlight, shared TTS |
| `src/components/jarvis/onboarding/SpotlightOverlay.tsx` | Spotlight — exports activeTTSAudio + stopActiveTTS |

---

## Key Reminders

- PAUL lives at `jarvis/.paul/` (not repo root)
- No local dev — push to GitHub → auto-deploys to live site
- Calendar IS working — don't re-investigate (7 events confirmed)
- Jenny's calendar = Awesome Calendar (iCloud), not Google Calendar
- `ELEVENLABS_API_KEY` already in Vercel — never ask again
- All Google Calendar env vars set — never ask again

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Run `/paul:plan` to write L-02-05-03 (safe-area fix first, voice touch-and-talk second)
3. Apply and then `/paul:unify` all L-02-05 plans

*Handoff: 2026-03-05 — session pause after L-02-05-02 deployed*
