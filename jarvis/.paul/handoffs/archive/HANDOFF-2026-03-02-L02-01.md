# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — L-02-01 plan created, awaiting approval

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis — Self-Improving Life Manager
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-02 of 4 — Live Walkthrough Pass 1
**Plan:** L-02-01 — Pre-Walkthrough Diagnostic Fixes (created, awaiting approval)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [Plan created, awaiting approval]
```

---

## What Was Done

This session:
- Resumed from prior handoff, confirmed L-02 ready to plan
- Deep code audit of the entire onboarding → Academy → spotlight pipeline (8+ source files)
- Discovered 3 code-auditable bugs that would break the live walkthrough:
  1. **SpotlightOverlay dual-render bug** — `querySelector` finds hidden mobile DomainRail on desktop → zero-dimension rect → invisible spotlight. Both mobile and desktop nav have `data-tutorial-id="domain-rail"`, querySelector picks first (mobile, hidden).
  2. **Spotlight target ID mismatches** — Curriculum/system prompt reference `bottom-tab-academy` but DOM has `bottom-tab-learn`. System prompt references `tasks-first-checkbox-0` but DOM has `tasks-first-checkbox`.
  3. **AcademyHub ignores activeProject** — OnboardingWizard sets `academyStore.activeProject='jarvis'` but AcademyHub hardcodes initial tab to `'tutorials'`. User coming from onboarding sees wrong tab.
- Created L-02-01-PLAN.md with 3 tasks to fix all 3 issues
- Archived previous handoff to `.paul/handoffs/archive/`

Previous sessions (L-01 Foundation — 2 plans):
- L-01-01: Redirect `/jarvis` → `/jarvis/app`, 8-topic Jarvis curriculum, OnboardingWizard → Academy handoff
- L-01-02: Spotlight bridge (`spotlight_element` + `clear_spotlight` tools, SSE bridge, DOM wiring), SAME-ORIGIN TEACHING prompt, `startTour` differentiation

---

## What's In Progress

- Nothing in progress — clean pause at plan-approval boundary

---

## What's Next

**Immediate:** Approve and run `/paul:apply` for L-02-01 (Pre-Walkthrough Diagnostic Fixes)

The plan has 3 autonomous tasks:
1. Harden SpotlightOverlay — `querySelectorAll` + visible-element filter + `scrollIntoView`
2. Fix ID mismatches — `bottom-tab-academy` → `bottom-tab-learn`, `tasks-first-checkbox-0` → `tasks-first-checkbox`
3. Wire `activeProject` from academyStore into AcademyHub initial tab state

**After L-02-01:** Create L-02-02 plan — the live walkthrough checkpoint where Jonathan clears localStorage, walks through fresh, and we fix what breaks in real-time.

**After L-02:** L-03 (Pass 2: Calendar → Meals → Briefing → Chat) → L-04 (Polish + Wife Test)

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state — L-02 plan created |
| `.paul/ROADMAP.md` | Phase overview — v4.4 with 4 phases, 1 complete |
| `.paul/PROJECT.md` | Full project context with all key decisions |
| `.paul/phases/L-02-live-walkthrough-pass1/L-02-01-PLAN.md` | Plan to approve — 3 diagnostic fixes |
| `.paul/phases/L-01-foundation/L-01-02-SUMMARY.md` | Last completed plan — spotlight bridge |
| `.paul/concepts/guided-onboarding-v44.md` | Full concept doc for v4.4 |

---

## Key Context for Next Session

- **L-02-01 is a diagnostic fix plan** — not the walkthrough itself. It eliminates predictable failures so the walkthrough can focus on unpredictable ones (UX, timing, teaching quality).
- **SpotlightOverlay fix is global** — querySelectorAll + visible-filter solves dual-render for any component, not just DomainRail. Elegant approach.
- **ID fixes are reference-side** — DOM annotations are correct. Fix curriculum/prompt to match, not the other way around.
- **500ms setTimeout timing** — still untested. Will surface during live walkthrough (L-02-02).
- **PriorityStack dual IDs** — empty Card and populated ul both have `home-priority-stack`, but only one renders at a time. Should be fine with the new querySelectorAll approach.

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Loop position: PLAN ✓ — plan created, awaiting approval
3. Run `/paul:resume` or directly approve with `/paul:apply .paul/phases/L-02-live-walkthrough-pass1/L-02-01-PLAN.md`

---

*Handoff created: 2026-03-02*
