# PAUL Handoff

**Date:** 2026-03-02
**Status:** paused — apply complete, awaiting unify

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis — Self-Improving Life Manager
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-01 of 4 — Foundation
**Plan:** L-01-02 applied successfully

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [Applied, awaiting unify]
```

---

## What Was Done

This session:
- Resumed from handoff, confirmed L-01-02 plan ready for apply
- Executed L-01-02 plan: 2 tasks, 10 files modified, all verification checks pass
- **Task 1 — Spotlight tools + SSE bridge + DOM wiring:**
  - Added `spotlight_element` and `clear_spotlight` tool definitions to `tutorialTools.ts`
  - Added handler cases in `tutorial/toolExecutor.ts` (validation + response messages)
  - Updated `isTutorialTool()` guard function with new tool names
  - Registered both names in `chatProcessor.ts` `tutorialToolNames` Set (routes to tutorial executor)
  - Built SSE spotlight bridge in `ChatOverlay.tsx`: intercepts `tool_use` events for spotlight tools, calls `useTutorialStore.getState().setSpotlight()` / `.clearSpotlight()` directly (already imported)
  - Added `data-tutorial-id` attributes: DomainRail (both mobile+desktop nav + per-button), BottomTabBar nav container, PriorityStack (both empty and populated states)
- **Task 2 — Teaching prompt + topic enrichment + startTour:**
  - Added SAME-ORIGIN TEACHING section to `systemPrompt.ts` inside ACADEMY block (between TEACHING SESSION FLOW and READING CODE)
  - Enriched `handleListTopics()` in `academy/toolExecutor.ts` to append spotlightTargets for topics that have them
  - Differentiated `startTour()` in `OnboardingWizard.tsx`: calls `finishOnboarding()` then `setTimeout(() => useChatStore.getState().openWithMessage('Start my guided tour of Jarvis'), 500)`
  - Imported `useChatStore` in OnboardingWizard
  - Verified "Skip tour" path unchanged (bare `finishOnboarding()`)
- Build passes clean with zero errors

Previous sessions:
- L-01-01 shipped: redirect `/jarvis` → `/jarvis/app`, 8-topic Jarvis curriculum, OnboardingWizard → Academy handoff

---

## What's In Progress

- L-01-02 APPLY complete, UNIFY not yet run
- No code changes in progress — clean pause at apply boundary

---

## What's Next

**Immediate:** Run `/paul:unify` to reconcile L-01-02 plan vs actual

**After that:** Assess if L-01 Foundation needs more plans (L-01-03+) or ready for L-02 Live Walkthrough

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview — v4.4 with 4 phases |
| `.paul/phases/L-01-foundation/L-01-02-PLAN.md` | Plan that was applied — spotlight bridge + teaching context |
| `.paul/phases/L-01-foundation/L-01-01-SUMMARY.md` | Prior plan results — redirect + curriculum + handoff |
| `.paul/concepts/guided-onboarding-v44.md` | Full concept doc (sections 3, 7.2, 7.4 most relevant) |
| `src/lib/jarvis/tutorial/tutorialTools.ts` | spotlight_element + clear_spotlight tool definitions |
| `src/lib/jarvis/tutorial/toolExecutor.ts` | Tool handler cases + isTutorialTool guard |
| `src/lib/jarvis/intelligence/chatProcessor.ts` | tutorialToolNames routing set |
| `src/components/jarvis/layout/ChatOverlay.tsx` | SSE spotlight bridge (intercepts tool_use events) |
| `src/lib/jarvis/intelligence/systemPrompt.ts` | SAME-ORIGIN TEACHING section in ACADEMY block |
| `src/lib/jarvis/academy/toolExecutor.ts` | handleListTopics with spotlightTargets |
| `src/components/jarvis/onboarding/OnboardingWizard.tsx` | startTour → openWithMessage differentiation |

---

## Key Context for Next Session

- End-to-end flow: Claude calls `spotlight_element` → server validates → SSE sends `tool_use` event → ChatOverlay intercepts → `tutorialStore.setSpotlight()` → SpotlightOverlay renders highlight → auto-clears on click
- ChatOverlay already imported `useTutorialStore` (line 7) — no new dependency added
- `isTutorialTool()` in toolExecutor.ts updated (sometimes overlooked — it's a separate guard function)
- System prompt SAME-ORIGIN TEACHING section lists all available spotlight targets by category
- startTour uses 500ms setTimeout because finishOnboarding triggers router.push — shell chrome must mount before openWithMessage can auto-send
- Known consideration: dual-render components (DomainRail mobile nav + desktop nav) both get `data-tutorial-id="domain-rail"` — `querySelector` finds first in DOM (mobile), which is hidden on desktop. SpotlightOverlay would get zero-dimension rect. L-02 Live Walkthrough will test and fix if needed.
- Build passes clean. No type errors.

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Loop position: PLAN ✓, APPLY ✓, UNIFY ○
3. Run `/paul:unify` to reconcile L-01-02

---

*Handoff created: 2026-03-02*
