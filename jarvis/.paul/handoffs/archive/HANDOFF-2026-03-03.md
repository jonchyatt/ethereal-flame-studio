# PAUL Handoff

**Date:** 2026-03-03
**Status:** paused — ElevenLabs feature planned but not built, L-02 walkthrough still pending

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis v4.4 — Guided Onboarding (Academy-Driven)
**Core value:** Jonathan walks through Jarvis cold, gets guided by Academy spotlight + voice narration, exits knowing how to use the app. Exit criteria: wife can do the same.

---

## Current State

**Version:** v4.4 — Guided Onboarding
**Phase:** L-02 of 4 — Live Walkthrough Pass 1
**Plan:** L-02-02 — code deployed, live walkthrough NOT yet done

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○
          (deployed, walkthrough pending)
```

---

## What Was Done This Session

- **Codex (offline) reworked SpotlightOverlay.tsx** — laser pointer effect: red beam + animated dot traveling to target, high-contrast red border/glow. Commit `5742bdb`. Only file changed.
- **Bottom nav safe area fixed** — `safe-area-bottom` was a dead class, replaced with `pb-[env(safe-area-inset-bottom)]` on nav + inner div fixed to `h-16`. JarvisShell content padding updated to `calc(4rem + env(safe-area-inset-bottom))`. Commit `bb084d4`.
- **Build unblocked** — `drizzle-kit push` was hanging on interactive rename/create prompt in CI. Fixed with `printf '\n' | drizzle-kit push --force`. Commits `f02b4ce` + `7f26db9`. Build now deploys clean.
- **ElevenLabs env vars added to Vercel** — Jonathan added `ELEVENLABS_API_KEY` and `ELEVENLABS_VOICE_ID=MIepW0jee0xBaRFcpwSk` manually via Vercel dashboard.

---

## What's In Progress / Not Yet Built

### ElevenLabs + Tutorial Narration (PLANNED, 0% built)

7 files to change:

1. **`src/app/api/jarvis/tts/route.ts`** — swap Polly → ElevenLabs primary (`eleven_turbo_v2_5`, voice `MIepW0jee0xBaRFcpwSk`), keep Polly as fallback. Pattern: raw HTTPS call, `xi-api-key` header, returns mp3. Reference: `C:/Users/jonch/Projects/claudeclaw/src/voice.ts`.

2. **`src/lib/jarvis/stores/tutorialStore.ts`** — add `narration?: string` to `SpotlightTarget` interface. Add `isNarrationEnabled: boolean` (default `true`, persisted) + `toggleNarration()` action.

3. **`src/lib/jarvis/tutorial/tutorialTools.ts`** — add optional `narration` param to `spotlight_element` schema. Short spoken sentence Claude passes alongside the visual highlight.

4. **`src/lib/jarvis/tutorial/toolExecutor.ts`** — pass `narration` through in `spotlight_element` case.

5. **`src/components/jarvis/layout/ChatOverlay.tsx`** — include `narration: event.tool_input.narration` when calling `setSpotlight()`.

6. **`src/components/jarvis/onboarding/SpotlightOverlay.tsx`** — `useEffect` on `spotlight?.narration` + `isNarrationEnabled` → fetch `/api/jarvis/tts` → play MP3. Small speaker icon (fixed, top-right, only during active tutorial) to toggle mute. Auto-on by default.

7. **`src/lib/jarvis/intelligence/systemPrompt.ts`** — update `spotlight_element` docs to tell Claude to include a short spoken narration sentence.

### L-02 Live Walkthrough (still pending)
- 10 checkpoints to walk through manually on live site
- Known issue: phone chat instructions disappear before user can read them (investigate ChatOverlay — does startTour close the chat?)
- Checkpoint 8 = bottom nav "Learn" tab spotlight (`data-tutorial-id="bottom-tab-learn"`)
- After walkthrough: fix-as-we-go → `/paul:unify`

---

## What's Next

**Immediate:** Build the ElevenLabs + tutorial narration feature (7 files, one commit). User said "go" but session ended first. Just build it — no further approval needed.

**After that:** Live walkthrough of all 10 checkpoints on https://jarvis.whatamiappreciatingnow.com/ — clear localStorage first (`?reset=true`), walk all steps, fix what breaks, then unify L-02-02.

---

## Key Context

- **ElevenLabs voice:** `MIepW0jee0xBaRFcpwSk` (Jonathan's chosen voice, sourced from `claudeclaw/.env`)
- **ElevenLabs model:** `eleven_turbo_v2_5`
- **API pattern:** See `C:/Users/jonch/Projects/claudeclaw/src/voice.ts` — raw HTTPS, no SDK, stability 0.5, similarity_boost 0.75
- **Narration toggle:** auto-on, speaker icon to mute, persisted to localStorage via tutorialStore
- **`safe-area-bottom` is a dead class** — no Tailwind plugin installed. Use `pb-[env(safe-area-inset-bottom)]` everywhere.
- **Drizzle build:** `printf '\n' | drizzle-kit push --force` — do not regress this

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `src/app/api/jarvis/tts/route.ts` | TTS route (currently Polly, needs ElevenLabs) |
| `src/lib/jarvis/stores/tutorialStore.ts` | Tutorial state + spotlight |
| `src/lib/jarvis/tutorial/tutorialTools.ts` | spotlight_element tool schema |
| `src/components/jarvis/layout/ChatOverlay.tsx` | SSE tool_use handler |
| `src/components/jarvis/onboarding/SpotlightOverlay.tsx` | Visual spotlight + narration |
| `C:/Users/jonch/Projects/claudeclaw/src/voice.ts` | ElevenLabs reference implementation |

---

## Resume Instructions

1. Run `/paul:resume` to restore context
2. Build the 7-file ElevenLabs feature — user approved, just do it
3. Then do the live walkthrough

---

*Handoff created: 2026-03-03*
