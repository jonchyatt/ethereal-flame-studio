# PAUL Handoff

**Date:** 2026-02-28
**Status:** paused — H-01 applied, awaiting unify

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis v4.0 — Brain Swap & Life Manager UI
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Version:** v4.0 — Phases A-G COMPLETE, Phase H applied
**Phase:** H of H — Google Calendar Integration
**Plan:** H-01 APPLIED — code shipped to production (commit e22249a)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [Code shipped, awaiting unify]
```

---

## What Was Done This Session

### H-01 Applied: Google Calendar Integration (647 lines, 9 files)

**New files (2):**
- `src/lib/jarvis/google/GoogleCalendarClient.ts` (~230 lines) — Service account JWT auth via native crypto (zero new deps), token caching with 60s buffer, RFC 3339 timezone offset utility, event fetching with retry/circuit breaker
- `src/lib/jarvis/google/calendarToolExecutor.ts` (~110 lines) — `query_calendar` chat tool, 4 timeframes (today/tomorrow/this_week/next_week), RFC 3339 compliant boundaries

**Modified files (6):**
- `executive/types.ts` — CalendarEvent extended: `source` (required), `endTime?`, `allDay?`, `location?`, `googleEventId?`
- `executive/BriefingBuilder.ts` — 3 helpers added (transformGoogleCalendarEvents, mergeCalendarEvents, fetchGoogleCalendarEventsSafe), all 3 briefing builders enriched via parallel Promise.all
- `stores/personalStore.ts` — Store CalendarEvent: `allDay?`, `location?`, `source?`
- `hooks/useJarvisFetch.ts` — `transformCalendar()` passes through endTime, allDay, location, source
- `intelligence/tools.ts` — `calendarTools` array, `getAllTools()` includes `query_calendar`
- `intelligence/chatProcessor.ts` — 4-way tool routing (tutorial → memory → calendar → notion), MCP mode: `[...localOnlyTools, ...calendarTools]`

**Bug fixed:** Evening wrap `calendar.today` was incorrectly using `tomorrowTasks` — now uses `[...completedTasks, ...pendingTasks]` (today's tasks).

**All 3 audit fixes from prior session applied:** RFC 3339 compliance, evening wrap bug, MCP mode visibility.

**19/19 verification checks PASS. Build clean. Merged to master. Pushed to production.**

---

## What's Next

**Immediate:** Run `/paul:unify` to close the H-01 loop (reconcile plan vs actual, mark phase complete)

**After unify:** Jonathan needs to set up Google Cloud service account:
1. Create project in Google Cloud Console
2. Enable Google Calendar API
3. Create service account + JSON key
4. Share calendar with service account email
5. Add `GOOGLE_CALENDAR_SERVICE_ACCOUNT_JSON` + `GOOGLE_CALENDAR_ID` to Vercel env vars
6. Redeploy (or wait for next push)

**After Phase H:** All phases A-H complete. v4.0 milestone done. Decide v4.1 scope (Intelligence Evolution concepts in `.paul/concepts/`).

---

## Key Decisions Made

- **Service account over OAuth** — single-user read-only, no token refresh dance, no callback routes
- **Native crypto over googleapis SDK** — zero new dependencies
- **RFC 3339 offset via `getTimezoneOffsetString()`** — exported utility, uses Intl.DateTimeFormat
- **MCP mode: `[...localOnlyTools, ...calendarTools]`** — calendar is third category (external-but-not-Notion)
- **Evening wrap bug bundled with H-01** — pre-existing bug fixed since merge logic touches same code path
- **All-day events float to top** in merged sort, alphabetical among themselves
- **Auth failures (401/403) return empty array** — don't retry, don't trip circuit breaker

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview |
| `.paul/phases/H-google-calendar/H-01-PLAN.md` | Audited plan (reference) |
| `.paul/phases/H-google-calendar/H-01-SUMMARY.md` | Execution summary with verification results |

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Run `/paul:resume`
3. Next action: `/paul:unify` to close the H-01 loop

---

*Handoff created: 2026-02-28*
