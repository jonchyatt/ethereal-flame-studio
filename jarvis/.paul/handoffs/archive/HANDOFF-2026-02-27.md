# PAUL Handoff

**Date:** 2026-02-27
**Status:** paused — F-02 plan approved, ready for APPLY

---

## READ THIS FIRST

You have no prior context. This document tells you everything.

**Project:** Jarvis v4.0 — Brain Swap & Life Manager UI
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Version:** v4.0
**Phase:** F of G — Vector Memory
**Plan:** F-02 — Memory Consolidation (PLAN created, awaiting APPLY)

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ○        ○     [Plan created, awaiting APPLY]
```

**Milestone Progress:** ~90%
- Phases A-D: Complete (Intelligence Audit, SDK Integration, Memory Preservation, Self-Improvement)
- Phase E: ~90% (E-01 through E-06 shipped; E-07+ remaining domain UIs deferred)
- Phase F: 60% (F-01 done; F-02 planned, not yet executed)
- Phase G: Not started

---

## What Was Done This Session

- Ran `/paul:progress` — routed to F-02 planning
- Created initial F-02 plan (v1)
- User challenged plan quality — performed self-critique and identified 4 significant flaws
- **Revised F-02 plan (v2)** with these improvements:
  1. **Stored-embedding detection** — zero OpenAI API calls (was N calls)
  2. **Haiku synthesis** for merges — preserves all detail (was naive "keep longer")
  3. **Two-phase tool** (preview → confirm) — matches forget_fact pattern (was silent merge)
  4. **Re-embed after merge** — keeps search accurate (was stale embedding)

---

## What's In Progress

- Nothing in progress — plan is written and ready for execution

---

## What's Next

**Immediate:** `/paul:apply .paul/phases/F-vector-memory/F-02-PLAN.md`

Executes 2 tasks:
1. Create `consolidation.ts` (engine + Haiku synthesis) + add `updateMemoryContent()` to memoryEntries.ts
2. Wire `consolidate_memories` two-phase tool into toolExecutor + memoryTools + index

**After that:** `/paul:unify` to close the F-02 loop, then decide: Phase G (Integration & Polish) or remaining E-07+ UI work.

---

## Key Decisions This Session

| Decision | Rationale |
|----------|-----------|
| Use stored embeddings for candidate detection | Avoids N OpenAI API calls — embeddings already in memory_embeddings table |
| Haiku synthesis for merges | Cheap (~$0.001/merge), preserves specific details from both entries |
| Two-phase tool (preview → confirm) | Matches forget_fact UX pattern — user sees what will merge before it happens |
| Re-embed after merge | Synthesized content differs from original — stale embedding would hurt search |
| Threshold 0.15 default | Conservative for text-embedding-3-small cosine distance |
| No auto-trigger | Tool-invoked only for now — auto-trigger deferred to Phase G |

---

## Key Files

| File | Purpose |
|------|---------|
| `.paul/STATE.md` | Live project state |
| `.paul/ROADMAP.md` | Phase overview (A-G) |
| `.paul/phases/F-vector-memory/F-02-PLAN.md` | **The plan to execute next** |
| `.paul/phases/F-vector-memory/F-01-SUMMARY.md` | What F-01 built (embeddings, vector search, dual retrieval) |
| `src/lib/jarvis/memory/embeddings.ts` | Embedding infrastructure (F-01) |
| `src/lib/jarvis/memory/vectorSearch.ts` | Vector + dual search (F-01) |
| `src/lib/jarvis/memory/queries/memoryEntries.ts` | Memory CRUD + BM25 (will be modified — add updateMemoryContent) |
| `src/lib/jarvis/memory/toolExecutor.ts` | Tool routing (will be modified — add consolidate handler) |
| `src/lib/jarvis/intelligence/memoryTools.ts` | Tool definitions (will be modified — add consolidate_memories) |

---

## Outstanding Items

- OPENAI_API_KEY must be added to Vercel environment for production vector search (noted since F-01)
- E-07+ (remaining domain UI waves) not started
- Phase G (Integration & Polish) not started

---

## Resume Instructions

1. Read `.paul/STATE.md` for latest position
2. Check loop position (should be: PLAN ✓, APPLY ○, UNIFY ○)
3. Run `/paul:resume` or `/paul:apply .paul/phases/F-vector-memory/F-02-PLAN.md`

---

*Handoff created: 2026-02-27*
