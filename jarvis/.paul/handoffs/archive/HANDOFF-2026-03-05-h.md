# PAUL Handoff

**Date:** 2026-03-05 (session pause — context limit)
**Status:** paused — L-03 bug fixes applied, UNIFY pending

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Production:** https://jarvis.whatamiappreciatingnow.com/
**Core value:** One system that knows everything, surfaces what matters, keeps you on track.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-03 of 4 (Live Walkthrough Pass 2) — In Progress

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ○     [fixes applied, unify pending]
```

---

## What Was Done This Session

- **L-03-01 APPLIED** — Fixed `"meet-jarvis"` → `"welcome-tour"` in systemPrompt.ts. Commit 7e9d5d9.
- **L-03-01 CHECKPOINT** — Jonathan completed live walkthrough. Gave detailed bug report (8 bugs).
- **L-03-02/03 APPLIED** — Major UX bug fix batch. Commit fbd1459. Files changed:
  - `src/lib/jarvis/utils/audioUnlock.ts` — new shared iOS audio unlock singleton
  - `src/components/jarvis/layout/ChatOverlay.tsx` — 5 fixes
  - `src/components/jarvis/onboarding/SpotlightOverlay.tsx` — retry logic, mute button removed
  - `src/components/jarvis/layout/Header.tsx` — narration toggle added permanently
  - `src/components/jarvis/layout/JarvisShell.tsx` — body scroll lock + global audio unlock

---

## Bugs Fixed (commit fbd1459)

1. **Chat auto-scroll** — replaced `scrollIntoView` with `scrollTop = scrollHeight` in rAF (iOS fixed-container reliable)
2. **Chat input hidden behind iPhone home bar** — added `pb-[env(safe-area-inset-bottom)]` to ChatInputRow form
3. **"Continue" missing from quick replies** — added as first quick reply option
4. **Spotlight fails after navigation** — retry logic in `measure()`: polls up to 10× at 150ms until element appears in DOM
5. **Mute button disappears between spotlights** — moved from SpotlightOverlay to Header.tsx (always visible)
6. **iOS TTS stops on any touch/scroll** — body `overflow:hidden` during spotlight prevents scroll-pause; global `onTouchStart` on shell div unlocks audio before chat ever opens
7. **Jarvis restarts tour every session** — tutorial progress (explored/completed academy topics) now injected into `buildAppContext()` → Jarvis sees "Already taught: welcome-tour, tasks-basics..." and continues

---

## Bugs NOT Yet Fixed (need L-03-04+)

| Bug | Status | Notes |
|-----|--------|-------|
| Bottom padding (lists buried) | Needs live testing | Math looks correct — 80px+safe-area > 64px BottomTabBar. May be a non-issue or device-specific. |
| Academy locks gating content | Not started | Need to read LessonCard/tutorialLessons lock logic |
| Jarvis too verbose in text | Not started | System prompt update needed |
| Welcome-tour repeat IN same session | Partially fixed | appContext injection helps; may need more if academyStore isn't writing fast enough |

---

## What's Next

**Immediate:** Run UNIFY for L-03-02/03, then checkpoint Jonathan to test on live device.

Next command: `/paul:unify` (no formal PLAN.md exists for L-03-02/03 — unify informally or write summary directly to STATE.md)

**After that:**
1. Jonathan tests on live site (auto-deploy in ~1 min from last push)
2. Document which bugs are actually fixed, which still need work
3. Write L-03-04 plan for remaining bugs (verbosity, locks, padding if still broken)

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `src/components/jarvis/layout/ChatOverlay.tsx` | Chat panel — auto-scroll, quick replies, appContext |
| `src/components/jarvis/onboarding/SpotlightOverlay.tsx` | Spotlight + retry logic |
| `src/components/jarvis/layout/Header.tsx` | Narration mute toggle now lives here |
| `src/components/jarvis/layout/JarvisShell.tsx` | Body scroll lock + global audio unlock |

---

## Key Reminders

- PAUL lives at `jarvis/.paul/` (not repo root)
- No local dev — push to GitHub → auto-deploys → test from live site
- `ELEVENLABS_API_KEY` already in Vercel — never ask again
- Voice/PushToTalk: NOT in L-03 scope

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Run `/paul:resume`
3. Unify L-03-02/03 (write SUMMARY or note in STATE.md that no formal PLAN.md exists — changes were applied directly)
4. Checkpoint Jonathan to test on live site

*Handoff: 2026-03-05 — session pause at context limit*
