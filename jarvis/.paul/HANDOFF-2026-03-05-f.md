# PAUL Handoff

**Date:** 2026-03-05 (session pause after plan creation)
**Status:** paused — L-02-05-03 plan written, ready to apply

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Production:** https://jarvis.whatamiappreciatingnow.com/
**Core value:** One system that knows everything, surfaces what matters, keeps you on track, and gets smarter over time.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-02 of 4 (Live Walkthrough Pass 1) — In Progress
**Plan:** L-02-05-03 PLAN written, not yet applied

**Loop Position:**
```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [L-02-05-01 complete]
  ✓        ✓        ✓     [L-02-05-02 complete]
  ✓        ○        ○     [L-02-05-03 plan created, awaiting apply]
```

---

## What Was Done This Session

### Planning L-02-05-03
- Read and analyzed JarvisShell.tsx, BottomTabBar.tsx, personal/TasksList.tsx, ChatOverlay.tsx, PushToTalk.tsx
- **Root cause found**: `TutorialContinueButton` (fixed, ~40px tall) sits at `bottom: calc(5rem + safe-area + 0.75rem)` = 92px + safe-area from screen bottom. Its top edge is at ~132px + safe-area. But `<main>` only has `pb: 80px + safe-area` → **52px of content hidden behind the button** when tutorial is active.
- Voice deferred: `PushToTalk` component exists and has full press-and-hold gesture logic + STT route exists at `/api/jarvis/stt`, but `PushToTalk` is NOT integrated into `ChatOverlay` — it would require building a mic button + audio recording + STT call. New feature, not bug fix. Explicitly out of scope for L-02-05-03.
- Wrote `L-02-05-03-PLAN.md` — single task, single file (`JarvisShell.tsx`), zero new imports

---

## Jenny Demo Checklist

1. ✅ Bills show (static fallback)
2. ✅ Calendar shows when events exist
3. ✅ Tutorial runs all 4 lessons without stopping (L-02-05-01)
4. ✅ Tutorial UX smooth — no double TTS, chat closes for spotlight (L-02-05-02)
5. ⬜ Tasks list visible — not buried under Continue button (L-02-05-03)
6. ⬜ Voice touch-and-talk UX (deferred — separate plan needed)

---

## What's Next

**Immediate:** Apply L-02-05-03

The plan is tiny — 1 task, 1 file:
1. In `JarvisShell.tsx`, add `const suggestedNext = useTutorialStore((s) => s.suggestedNext);`
2. Compute `const continueBtnVisible = tutorialEngine.isActive || !!suggestedNext;`
3. Change `<main>` className to use conditional pb:
   - `pb-[calc(10rem_+_env(safe-area-inset-bottom))]` when `continueBtnVisible`
   - `pb-[calc(5rem_+_env(safe-area-inset-bottom))]` otherwise
   - `md:pb-4` stays unchanged (overrides on desktop)

**After apply:** Run `/paul:unify` on all three L-02-05 plans (01+02+03), then assess L-02 completion. If tutorial is working end-to-end, move to L-03.

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `jarvis/.paul/phases/L-02-live-walkthrough-pass1/L-02-05-03-PLAN.md` | The plan to apply |
| `src/components/jarvis/layout/JarvisShell.tsx` | Only file to change |

---

## Key Reminders

- PAUL lives at `jarvis/.paul/` (not repo root)
- No local dev — push to GitHub → auto-deploys
- Calendar IS working — 7 events confirmed. Don't re-investigate.
- Jenny's calendar = Awesome Calendar (iCloud), NOT Google Calendar
- `ELEVENLABS_API_KEY` already in Vercel — never ask again
- All Google Calendar env vars set — never ask again
- `useTutorialStore` is ALREADY imported in JarvisShell — zero new imports needed
- `tutorialEngine` already exists in JarvisShell from `useTutorialEngine()` — just add `suggestedNext` selector

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Run `/paul:apply jarvis/.paul/phases/L-02-live-walkthrough-pass1/L-02-05-03-PLAN.md`
3. Then `/paul:unify` all L-02-05 plans

*Handoff: 2026-03-05 — session pause after L-02-05-03 plan written*
