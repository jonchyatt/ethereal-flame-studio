# PAUL Handoff

**Date:** 2026-03-06 (context limit)
**Status:** paused — local deployment activated, critical bug fixed, chat verification pending

---

## READ THIS FIRST

**Project:** Jarvis — Self-Improving Life Manager
**Production:** https://jarvis.whatamiappreciatingnow.com/
**Core value:** $0 brain cost via Claude Code SDK on local machine through Cloudflare tunnel.

---

## What Was Done This Session

### Local Deployment Activated (was always pending, never done)
- PM2 installed globally (`npm install -g pm2`)
- cloudflared downloaded to `C:\Users\jonch\AppData\Local\cloudflared.exe`
- All Vercel production env vars pulled to `.env.local` (via `vercel env pull --environment production`)
- `npm run build` succeeded clean
- PM2 started with 3 processes: jarvis-web (port 3001), jarvis-mcp, jarvis-cron
- Cloudflare tunnel created: `jarvis` (ID: `aab095b3-6382-4c75-8fe4-531e906538e1`)
- DNS switched: `jarvis.whatamiappreciatingnow.com` CNAME → tunnel (was Vercel)
- Tunnel config: `C:\Users\jonch\.cloudflared\config.yml`
- Tunnel running as background bash process (NOT managed by PM2 yet — see below)

### Critical Bug Fixed (commit 1f8d97d)
`providerRouter.ts` `getBrainProvider()` had NO check for `'claude-code-sdk'` — always fell through to `'anthropic-api'` default regardless of env var. Fixed by adding:
```typescript
if (env === 'claude-code-sdk' || env === 'sdk') return 'claude-code-sdk';
```
Also fixed `ecosystem.config.js` Windows PM2 issue: added `interpreter: 'cmd', interpreter_args: '/c'` to all 3 processes.

Both fixes committed and pushed to master (1f8d97d). Vercel auto-deployed.

### ecosystem.config.js Windows fix
PM2 on Windows can't run `npm`/`npx` directly — must use `interpreter: 'cmd', interpreter_args: '/c'`.

---

## Current State

**Milestone:** v4.4 Guided Onboarding — Academy-Driven
**Phase:** L-03 of 4 (Live Walkthrough Pass 2) — In Progress
**Loop:** PLAN ✓, APPLY ✓, UNIFY ✓ (L-03-01 and L-03-02/03 both unified this session)

```
PLAN ──▶ APPLY ──▶ UNIFY
  ✓        ✓        ✓     [loop closed — ready for L-03-04]
```

---

## Root Cause Found (DB Issue)

Direct node test: `DB OK` — Turso connects fine when `.env.local` loaded by dotenv directly.
Problem: Next.js running under PM2 `cmd /c npm start` is NOT receiving the env vars.
`.env.local` has 61 vars with quoted values — Next.js env loading under PM2/Windows isn't picking them up.

**Fix to implement next session:** Create `jarvis/scripts/start-web.bat` that explicitly loads `.env.local` vars before `npm start`. Replace PM2 script/interpreter with this bat file.

Or simpler: write a `jarvis/.env.production.local` (no quotes) that PM2 can read cleanly.

## What's In Progress / Unverified

### Chat verification
Jonathan got HTTP 500 before the router bug was fixed. After fixing and rebuilding, the server was restarted. **Jonathan has NOT yet confirmed chat works with SDK.** This is the first thing to verify on resume.

### Cloudflare tunnel not in PM2
The tunnel is running as a background process started via bash `&`. It will die if that terminal closes or machine reboots. Needs to be added to PM2 or set up as a Windows service.

---

## What's Next

**Immediate:** Verify chat works — open `jarvis.whatamiappreciatingnow.com`, send a message, confirm no 500 error and Jarvis responds.

**After that (in order):**
1. Add cloudflared to PM2 as a 4th process so it survives reboots
2. Set `pm2 startup` so PM2 itself survives reboots
3. If chat still fails — check `pm2 logs jarvis-web` and look for SDK errors (claude CLI auth, missing session, etc.)
4. Plan L-03-04 (remaining bugs: bottom padding, Academy locks, Jarvis verbosity)

---

## Infrastructure Summary

| Component | Location | Status |
|-----------|----------|--------|
| Next.js (Jarvis) | localhost:3001 via PM2 | Running |
| cloudflared tunnel | background bash process | Running (fragile — needs PM2) |
| PM2 | system-wide | Running, 0 restarts |
| Brain provider | Claude Code SDK | Configured (env var + code fix) |
| DNS | jarvis.whatamiappreciatingnow.com → tunnel | Active |
| Vercel | still deployed but DNS bypassed | Dormant |

---

## Key Files

| File | Purpose |
|------|---------|
| `jarvis/.paul/STATE.md` | Live project state |
| `jarvis/ecosystem.config.js` | PM2 process config (Windows fixed) |
| `C:\Users\jonch\.cloudflared\config.yml` | Tunnel config |
| `C:\Users\jonch\.cloudflared\aab095b3-...json` | Tunnel credentials |
| `C:\Users\jonch\AppData\Local\cloudflared.exe` | cloudflared binary |
| `src/lib/jarvis/intelligence/providerRouter.ts` | Brain provider routing (bug fixed) |
| `.env.local` | All production env vars (pulled from Vercel) |

---

## Add Cloudflared to PM2 (do this on resume)

Add to `jarvis/ecosystem.config.js` apps array:
```js
{
  name: 'jarvis-tunnel',
  script: 'C:\\Users\\jonch\\AppData\\Local\\cloudflared.exe',
  args: 'tunnel run jarvis',
  autorestart: true,
  watch: false,
},
```
Then: `pm2 restart jarvis-tunnel` or `pm2 start jarvis/ecosystem.config.js --only jarvis-tunnel`

Then run `pm2 save` and `pm2 startup` for reboot persistence.

---

## Resume Instructions

1. Read `jarvis/.paul/STATE.md`
2. Run `/paul:resume`
3. FIRST: verify chat works at `jarvis.whatamiappreciatingnow.com`
4. THEN: add cloudflared to PM2

*Handoff: 2026-03-06 — context limit*
