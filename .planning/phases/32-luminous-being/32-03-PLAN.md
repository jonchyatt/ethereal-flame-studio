---
phase: 32-luminous-being
plan: 03
type: execute
wave: 3
depends_on: [32-02]
files_modified:
  - blender/presets/luminous_being.json
autonomous: true
requirements: [LUMI-06]

must_haves:
  truths:
    - "All four luminous being layers respond to different audio frequency bands simultaneously"
    - "Bass drives volumetric fill density, mid drives particle emission, treble drives corona glow, onsets trigger fire wisps"
    - "The complete effect plays back as a coherent audio-reactive being of light"
  artifacts:
    - path: "blender/presets/luminous_being.json"
      provides: "Audio mapping preset with per-layer frequency separation for all luminous being targets"
      contains: "luminous_being"
  key_links:
    - from: "blender/presets/luminous_being.json"
      to: "blender/scripts/keyframe_generator.py"
      via: "preset loaded by apply_audio_keyframes()"
      pattern: "source_feature.*target_object"
    - from: "blender/presets/luminous_being.json"
      to: "blender/scripts/luminous_being_template.py"
      via: "apply_luminous_audio() passes preset_name='luminous_being'"
      pattern: "luminous_being"
---

<objective>
Create the luminous_being.json audio mapping preset that wires all four visual effect layers to different audio frequency bands for emergent complexity. This completes the Luminous Being effect -- every layer responds independently to the music.

Purpose: Without audio-reactive wiring, the Luminous Being is a static glow. The preset transforms it into a living, breathing entity that pulses, flickers, and radiates in sync with music. Per-layer frequency separation (bass=volumetric, mid=particles, treble=corona, onsets=fire) creates the emergent complexity principle established in Phase 27.

Output: blender/presets/luminous_being.json with 12+ simultaneous audio mappings across all four effect layers.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-luminous-being/32-CONTEXT.md
@.planning/phases/32-luminous-being/32-02-SUMMARY.md

@blender/presets/fire_cinema.json
@blender/presets/edm_lights.json
@blender/scripts/keyframe_generator.py
@blender/scripts/luminous_being_template.py

<interfaces>
<!-- From keyframe_generator.py -- mapping format: -->
```json
{
  "source_feature": "envelopes.bass",
  "target_object": "efs_lumi_fill",
  "target_property": "density",
  "data_path": "active_material.node_tree.nodes[\"Principled Volume\"].inputs[\"Density\"].default_value",
  "curve_type": "BEZIER",
  "scale": 5.0,
  "offset": 2.0,
  "clamp_min": 1.0,
  "clamp_max": 8.0
}
```

<!-- Available audio features (from audio JSON): -->
Source features:
- bands: sub_bass, bass, low_mid, mid, upper_mid, presence, brilliance, air
- envelopes: sub_bass, bass, low_mid, mid, upper_mid, presence, brilliance, air
- onsets: sub_bass, bass, low_mid, mid, upper_mid, presence, brilliance, air
- rms_energy, spectral_centroid, lufs, crest_factor, beat_phase, chromagram

<!-- Luminous Being target objects (from Plan 02): -->
Objects and keyframeable properties:
- efs_lumi_fill (Principled Volume): Density, Emission Strength, Emission Color (via Blackbody Intensity)
- efs_lumi_body (particle system): particle count (not directly keyframeable per-frame, but emission strength on particle material IS)
- efs_lumi_fire_domain: flame_max_temp (domain_settings)
- efs_lumi_fire_flow (which is efs_lumi_body with flow modifier): fuel_amount, temperature (flow_settings)
- efs_lumi_corona (Emission shader): Emission Strength via Fresnel chain
- efs_lumi_rim_light: energy (data.energy)

<!-- Resolve_target patterns from keyframe_generator.py: -->
Pattern 1: modifiers["Fluid"].domain_settings.{prop}
Pattern 2: modifiers["Fluid"].flow_settings.{prop}
Pattern 3: active_material.node_tree.nodes["NodeName"].inputs["InputName"].default_value
Pattern 4: data.energy
Pattern 5: data.color
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create luminous_being.json audio mapping preset</name>
  <files>blender/presets/luminous_being.json</files>
  <read_first>blender/presets/fire_cinema.json (mapping format reference with 11 mappings), blender/presets/edm_lights.json (dynamic_range section pattern), blender/scripts/keyframe_generator.py (resolve_target patterns -- lines 174-412 for all data_path patterns), blender/scripts/luminous_being_template.py (object names and material node names created in Plan 02)</read_first>
  <action>
Create blender/presets/luminous_being.json following the exact format of fire_cinema.json and edm_lights.json.

Preset metadata:
- name: "Luminous Being"
- description: "Crown jewel audio mapping: per-layer frequency separation with bass->volumetric fill, mid->particles, treble->corona, onsets->fire wisps. 14 simultaneous mappings for emergent complexity."
- audio_style: "all"
- auto_classify_hints: {} (works with any music style)

FREQUENCY BAND ALLOCATION (the core design):
- Sub-bass + Bass -> Volumetric fill (density, emission) -- deep breathing rhythm
- Low-mid + Mid -> Particle emission intensity -- textural mid-range energy
- Upper-mid + Presence -> Fire wisp fuel/temperature -- percussive accent
- Treble + Brilliance -> Corona edge glow -- high-frequency shimmer
- RMS energy -> Overall brightness across all layers
- Spectral centroid -> Color temperature shifts
- Onsets (bass) -> Fire wisp fuel bursts (CONSTANT curve for instant spikes)

14 MAPPINGS (specify all fields for each):

1. Bass envelope -> efs_lumi_fill Density
   - source_feature: "envelopes.bass"
   - target_object: "efs_lumi_fill"
   - target_property: "density"
   - data_path: "active_material.node_tree.nodes[\"Principled Volume\"].inputs[\"Density\"].default_value"
   - curve_type: "BEZIER", scale: 6.0, offset: 1.5, clamp_min: 0.5, clamp_max: 8.0

2. Sub-bass envelope -> efs_lumi_fill Emission Strength
   - source_feature: "envelopes.sub_bass"
   - target_object: "efs_lumi_fill"
   - target_property: "emission_strength"
   - data_path: "active_material.node_tree.nodes[\"Principled Volume\"].inputs[\"Emission Strength\"].default_value"
   - curve_type: "BEZIER", scale: 8.0, offset: 2.0, clamp_min: 1.0, clamp_max: 10.0

3. RMS energy -> efs_lumi_fill Blackbody Intensity (overall brightness)
   - source_feature: "rms_energy"
   - target_object: "efs_lumi_fill"
   - target_property: "blackbody_intensity"
   - data_path: "active_material.node_tree.nodes[\"Principled Volume\"].inputs[\"Blackbody Intensity\"].default_value"
   - curve_type: "BEZIER", scale: 1.5, offset: 0.3, clamp_min: 0.1, clamp_max: 1.5

4. Mid envelope -> particle material Emission Strength (efs_lumi_particle_mat_flame or current mode)
   - source_feature: "envelopes.mid"
   - target_object: "efs_lumi_body"
   - target_property: "particle_emission"
   - data_path: "active_material.node_tree.nodes[\"Emission\"].inputs[\"Strength\"].default_value"
   - curve_type: "BEZIER", scale: 5.0, offset: 0.5, clamp_min: 0.2, clamp_max: 6.0
   NOTE: This targets the particle render object's emission material, but since the body mesh is the emitter, keyframe_generator will find the active material on efs_lumi_body. If the body mesh active material is not the particle material, this mapping may need to target the particle render instance object instead. The executor should verify which object holds the particle emission material and adjust target_object accordingly.

5. Low-mid envelope -> particle material Emission Strength secondary modulation
   - source_feature: "envelopes.low_mid"
   - target_object: "efs_lumi_body"
   - target_property: "particle_emission_secondary"
   - data_path: "active_material.node_tree.nodes[\"Emission\"].inputs[\"Strength\"].default_value"
   - curve_type: "BEZIER", scale: 3.0, offset: 1.0, clamp_min: 0.5, clamp_max: 4.0
   NOTE: Same consideration as mapping 4 -- executor should verify target_object.

6. Bass onset -> fire wisp fuel burst (CONSTANT for instant spike)
   - source_feature: "onsets.bass"
   - target_object: "efs_lumi_body"
   - target_property: "fuel_amount"
   - data_path: "modifiers[\"Fluid\"].flow_settings.fuel_amount"
   - curve_type: "CONSTANT", scale: 2.0, offset: 0.0, clamp_min: 0.0, clamp_max: 3.0

7. Upper-mid envelope -> fire wisp fuel amount (sustained)
   - source_feature: "envelopes.upper_mid"
   - target_object: "efs_lumi_body"
   - target_property: "fuel_amount_sustained"
   - data_path: "modifiers[\"Fluid\"].flow_settings.fuel_amount"
   - curve_type: "BEZIER", scale: 1.5, offset: 0.3, clamp_min: 0.1, clamp_max: 2.0

8. Presence envelope -> fire wisp temperature
   - source_feature: "envelopes.presence"
   - target_object: "efs_lumi_body"
   - target_property: "temperature"
   - data_path: "modifiers[\"Fluid\"].flow_settings.temperature"
   - curve_type: "BEZIER", scale: 2.5, offset: 0.5, clamp_min: 0.3, clamp_max: 3.0

9. Spectral centroid -> fire wisp flame_max_temp (color temperature)
   - source_feature: "spectral_centroid"
   - target_object: "efs_lumi_fire_domain"
   - target_property: "flame_max_temp"
   - data_path: "modifiers[\"Fluid\"].domain_settings.flame_max_temp"
   - curve_type: "BEZIER", scale: 3.0, offset: 1.0, clamp_min: 0.5, clamp_max: 4.0

10. Treble envelope -> corona Emission Strength
    - source_feature: "envelopes.treble"
    - target_object: "efs_lumi_corona"
    - target_property: "emission_strength"
    - data_path: "active_material.node_tree.nodes[\"Emission\"].inputs[\"Strength\"].default_value"
    - curve_type: "BEZIER", scale: 6.0, offset: 1.0, clamp_min: 0.5, clamp_max: 8.0

11. Brilliance envelope -> corona Emission Strength (additive high shimmer)
    - source_feature: "envelopes.brilliance"
    - target_object: "efs_lumi_corona"
    - target_property: "emission_strength_shimmer"
    - data_path: "active_material.node_tree.nodes[\"Emission\"].inputs[\"Strength\"].default_value"
    - curve_type: "BEZIER", scale: 4.0, offset: 0.5, clamp_min: 0.3, clamp_max: 6.0

12. RMS energy -> rim light energy (subtle fill follows overall loudness)
    - source_feature: "rms_energy"
    - target_object: "efs_lumi_rim_light"
    - target_property: "energy"
    - data_path: "data.energy"
    - curve_type: "BEZIER", scale: 50.0, offset: 10.0, clamp_min: 5.0, clamp_max: 60.0

13. Spectral centroid -> rim light color temperature
    - source_feature: "spectral_centroid"
    - target_object: "efs_lumi_rim_light"
    - target_property: "color_temp"
    - data_path: "data.color"
    - curve_type: "BEZIER", scale: 1.0, offset: 0.0, clamp_min: 0.0, clamp_max: 1.0

14. Envelopes air -> corona secondary (ultra-high shimmer)
    - source_feature: "envelopes.air"
    - target_object: "efs_lumi_corona"
    - target_property: "emission_air"
    - data_path: "active_material.node_tree.nodes[\"Emission\"].inputs[\"Strength\"].default_value"
    - curve_type: "BEZIER", scale: 3.0, offset: 0.3, clamp_min: 0.1, clamp_max: 5.0

DYNAMIC RANGE section (same pattern as edm_lights.json):
- breakdown_rms_threshold: 0.12 (slightly more sensitive than EDM 0.15 -- luminous being should dim more noticeably in quiet passages)
- breakdown_sustain_frames: 20 (longer than EDM 15 -- being should gradually dim, not snap to dark)
- breakdown_multiplier: 0.15 (being dims to 15% during breakdowns -- not total darkness, still visible as faint glow)
- drop_multiplier: 1.0 (full brightness on drops)
- targets: volumetric fill density, fire wisp fuel, corona emission (the 3 most visually impactful targets from the main mappings list, with CONSTANT curve_type for instant overwrite)

Format the JSON with 2-space indentation matching existing presets.
  </action>
  <acceptance_criteria>
- File blender/presets/luminous_being.json exists
- Valid JSON (parseable)
- Contains "name": "Luminous Being"
- Contains "mappings" array with 12+ entries
- Contains "dynamic_range" object with breakdown_rms_threshold, breakdown_sustain_frames, breakdown_multiplier, drop_multiplier, targets
- Contains mappings targeting efs_lumi_fill (volumetric fill)
- Contains mappings targeting efs_lumi_corona (corona edge glow)
- Contains mappings targeting efs_lumi_fire_domain (fire wisps domain)
- Contains mappings targeting efs_lumi_body (fire flow + particles)
- Contains mappings targeting efs_lumi_rim_light (rim light)
- Contains at least one mapping with "onsets.bass" source (fire burst trigger)
- Contains at least one mapping with "envelopes.treble" source (corona driver)
- Contains at least one mapping with curve_type "CONSTANT" (onset-driven)
- All data_path values match patterns supported by keyframe_generator.py resolve_target()
  </acceptance_criteria>
  <verify>
    <automated>python -c "
import json
with open('C:/Users/jonch/Projects/ethereal-flame-studio/blender/presets/luminous_being.json') as f:
    p = json.load(f)
assert p['name'] == 'Luminous Being', f'Wrong name: {p[\"name\"]}'
m = p['mappings']
assert len(m) >= 12, f'Only {len(m)} mappings (need 12+)'
targets = set(x['target_object'] for x in m)
required_targets = {'efs_lumi_fill', 'efs_lumi_corona', 'efs_lumi_fire_domain', 'efs_lumi_body', 'efs_lumi_rim_light'}
missing = required_targets - targets
assert not missing, f'Missing targets: {missing}'
sources = set(x['source_feature'] for x in m)
assert any('onsets' in s for s in sources), 'No onset source'
assert any('treble' in s for s in sources), 'No treble source'
assert any(x.get('curve_type') == 'CONSTANT' for x in m), 'No CONSTANT curve'
dr = p.get('dynamic_range', {})
assert 'breakdown_rms_threshold' in dr, 'Missing dynamic_range'
assert 'targets' in dr and len(dr['targets']) >= 3, 'Need 3+ dynamic_range targets'
print(f'PASS: luminous_being.json has {len(m)} mappings across {len(targets)} targets with dynamic_range')
"</automated>
  </verify>
  <done>luminous_being.json preset exists with 14 mappings across all 5 target objects, per-layer frequency separation (bass->fill, mid->particles, treble->corona, onsets->fire), dynamic range with breakdown dimming, and all data_paths compatible with keyframe_generator.py</done>
</task>

</tasks>

<verification>
- luminous_being.json is valid JSON with 12+ mappings
- All target objects match efs_lumi_* names from luminous_being_template.py
- All data_path patterns match resolve_target() supported patterns in keyframe_generator.py
- Frequency separation: bass->fill, mid->particles, upper-mid/presence->fire, treble->corona
- Dynamic range section follows edm_lights.json pattern
- apply_luminous_audio() from template can load this preset via preset_name="luminous_being"
</verification>

<success_criteria>
1. luminous_being.json loads successfully via keyframe_generator.load_preset("luminous_being")
2. All 14 mappings target real objects created by luminous_being_template.py
3. Per-layer frequency separation creates emergent complexity (each layer responds to different bands)
4. Dynamic range causes the being to dim during quiet passages and brighten on drops
5. The complete Luminous Being effect is audio-reactive with all four visual layers responding simultaneously
</success_criteria>

<output>
After completion, create `.planning/phases/32-luminous-being/32-03-SUMMARY.md`
</output>
